import { Component, OnInit } from '@angular/core';
import {Router} from "@angular/router";

@Component({
  selector: 'app-one-evento',
  templateUrl: './one-evento.component.html',
  styleUrls: ['./one-evento.component.scss'],
})
export class OneEventoComponent  implements OnInit {
  ngOnInit(): void {
  }

  //listaEventos: any[] = [];
  //listaRegaos: any[] = [];
  //constructor(private router: Router) { }
//
  //ngOnInit() {}
//
  //handleInput(event: any) {
  //  this.listaEventos = this.datoEvento.filter((item) =>
  //    item.nombreEvento.includes(event.target.value)
  //  );
  //}
//
  //redirectToEvent(eventId: number) {
  //  // Redireccionar a la página del evento seleccionado
  //  this.router.navigate(['/evento', eventId]);
  //}

  // getEventos(){
  // return[{}]
  // }
  //
  // getRegalos(){
  // return[{}]
  // }
  //
  //
  //
}
